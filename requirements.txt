# Barely dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
typer>=0.9.0          # CLI framework
rich>=13.0.0          # Rich text formatting and tables
prompt-toolkit>=3.0.0 # Interactive REPL with autocomplete

# Blitz mode dependencies (audio visualization)
numpy>=1.24.0         # Audio processing
pyaudiowpatch>=0.2.0  # Windows audio capture (WASAPI loopback)
